fun main {
    var inpusr: Int? = null
    var increm = 1
    var sum = 0


        while (inpusr == null) {
            println("Please enter a number greater than zero:")
            var UsrInp = readln() 
        	try {
            		var inpusr = UsrInp?.toInt() {
                	if ($inpusr != null && $inpusr < 0) {
                    	print("Invalid entry. Please enter a NUMBER, GREATER THAN zero:")
                    	var inpusr = null
        }
} 		catch (e: Error) {
    println("Invalid entry. Please enter a number greater than zero.")
}
while (incrementor <= UI) {
    $sum += $increm
    $increm++
}
println("The sum of numbers between 1 $UI is $sum.")

