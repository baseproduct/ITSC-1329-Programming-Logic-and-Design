fun main ()
	println("What is the name of your favorite city?")
		val fav: String = readln()
		 	val favl = fav.length
			val fav2 = fav.toUpperCase()
				val index = 1
			val fav3 = fav.get(index)
			val fav4 = fav.toLowerCase()
	println("There are $fav1 characters in $fav. It starts with the letter $fav3. If your shift key is broken, it looks like $fav4, and if your caps lock is stuck, it looks like $fav2")